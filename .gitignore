vendor
.config-*
ssh_*
ansible_hosts*

# Ansible runtime cache and logs
.ansible/
ansible.log
failed_logs.txt

# Class/deployment artifacts generated by the Makefile workflow
TRAINING_FOLDER
TRAINING_TEMPLATE
all_instances_all_regions.txt

# Training SSH keys / key bundles (never commit secrets)
percona_training/
*_keys.zip
*.key
*.ppk

# Editor swap files
*.swp
